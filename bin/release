#!/usr/bin/env ruby

$:.unshift File.expand_path("../../lib", __FILE__)
require "language_pack"

$MYOUT.puts "Release: #{Time.now}"
$MYOUT.send :flush

LanguagePack::Instrument.trace 'release', 'app.release' do
  STDOUT.puts LanguagePack::Base.release
end

$MYOUT.send :flush
